## Agent skills

### Issue tracker

GitHub Issues — 使用 `gh issue` CLI 操作 `github.com/szb-66/EMMM`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个标准标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`，与 Matt Pocock 技能中的角色名一致。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文 — `CONTEXT.md` + `docs/adr/`，配合 `openspec/specs/` 作为技术真相来源。详见 `docs/agents/domain.md`。
